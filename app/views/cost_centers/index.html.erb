<p id="notice"><%= notice %></p>

<h1>Cost Centers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Branch</th>
      <th>Company</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cost_centers.each do |cost_center| %>
      <tr>
        <td><%= cost_center.name %></td>
        <td><%= cost_center.branch %></td>
        <td><%= cost_center.company %></td>
        <td><%= cost_center.status %></td>
        <td><%= link_to 'Show', cost_center %></td>
        <td><%= link_to 'Edit', edit_cost_center_path(cost_center) %></td>
        <td><%= link_to 'Destroy', cost_center, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cost Center', new_cost_center_path %>
